{namespace ClayLightsaber}

/**
 * This renders the component's whole content.
 */
{template .render}
	{@param? _handleHiltClick: any}
	{@param? active: bool}
	{@param? color: string}
	{@param? contentRenderer: string}
	{@param? elementClasses: string}
	{@param? id: string}

	{let $attributes kind="attributes"}
		class="clay-lightsaber
			{if $active}
				{sp}active
			{/if}

			{if $elementClasses}
				{sp}{$elementClasses}
			{/if}
		"

		{if $id}
			id="{$id}"
		{/if}
	{/let}

	<div {$attributes}>
		{delcall ClayLightsaber.Hilt variant="$contentRenderer"}
			{param _handleHiltClick: $_handleHiltClick /}
		{/delcall}

		{call .plasma}
			{param color: $color ?: 'blue' /}
		{/call}
	</div>
{/template}

/**
 * Hilt
 */
{deltemplate ClayLightsaber.Hilt}
	{@param _handleHiltClick: any}	

	<button class="hilt" data-onclick="{$_handleHiltClick}"></button>
	<div class="switch"></div>
{/deltemplate}

/**
 * Plasma
 */
{template .plasma}
	{@param color: string}

	<div class="plasma {$color}"></div>
{/template}